<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyAccount"
            android:label="Account Setting"
            android:parentActivityName=".PatientActivity" />
        <activity
            android:name=".Messages"
            android:label="Messages" />
        <activity
            android:name=".DoctorDetails"
            android:label="Doctor Details"
            android:parentActivityName=".FindDoctor" />
        <activity
            android:name=".FindDoctor"
            android:label="Available Doctors"
            android:parentActivityName=".PatientActivity" />
        <activity
            android:name=".FoodTracker"
            android:label="Food Tracker"
            android:parentActivityName=".PatientActivity" />
        <activity
            android:name=".Register"
            android:label="Patient Registration"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UpdateAdminActivity" />
        <activity
            android:label="Doctor Details"
            android:parentActivityName=".DoctorActivity"
            android:name=".UpdateDocActivity" />
        <activity android:name=".DoctorActivity" />
        <activity
            android:name=".AdminActivity"
            android:label="Admin Dashboard" />
        <activity android:name=".CashierActivity" />
        <activity
            android:name=".PatientActivity"
            android:label="Patient&apos;s Dashboard" />
        <activity
            android:name=".WelcomeScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>